################################################################################
# Library
################################################################################
add_library(consteig_array INTERFACE)
target_include_directories(consteig_array INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
